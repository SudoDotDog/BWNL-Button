/**
 * @author WMXPY
 * @namespace Button
 * @description DOCZ_RC
 */

export default {
    title: 'BWNL Button',
    filterComponents: (files) =>
        files.filter(filePath => /[w-]*.(js|jsx|ts|tsx)$/.test(filePath)),
    codeSandbox: false,
    typescript: true,
}
